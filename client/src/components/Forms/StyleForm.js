import { makeStyles } from "@material-ui/core";

export const formStyles = makeStyles({
  root: {
    flexGrow: 1,
    height: "100%",
  },
  formContainer: {
    flexGrow: 1,
    paddingTop: 60,
  },
  innerForm: {
    width: "inherit",
    textAlign: "center",
  },
  gridItemForm: {
    margin: "auto",
  },
  formButton: {
    paddingInline: 60,
    paddingBlock: 15,
    marginTop: 48,
  },
});
